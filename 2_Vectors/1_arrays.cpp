#include <iostream>

using std::cin;
using std::cout;
using std::endl;

int main()
{
    int array[1];
    array[0] = {150};

    cout << "Array on position 0 is: " << array[0] << endl;
    return 0;
}